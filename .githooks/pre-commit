#!/bin/sh

echo "Running Snake tests before commit..."
powershell -ExecutionPolicy Bypass -File "./scripts/run-tests.ps1"
status=$?

if [ "$status" -ne 0 ]; then
  echo ""
  echo "Pre-commit checks failed. Commit aborted."
  exit "$status"
fi

echo ""
echo "Pre-commit checks passed."
exit 0
